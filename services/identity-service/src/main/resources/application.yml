server:
  port: 8081
  servlet:
    context-path: /api

spring:
  application:
    name: identity-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
