package com.etribunal.gateway;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Test;

/** Placeholder — se reemplaza por tests de rutas del gateway en Fase 1. */
class SmokeTest {

    @Test
    void sanity() {
        assertThat(1 + 1).isEqualTo(2);
    }
}
