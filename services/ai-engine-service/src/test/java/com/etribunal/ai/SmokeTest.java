package com.etribunal.ai;

import static org.assertj.core.api.Assertions.assertThat;

import org.junit.jupiter.api.Test;

/** Placeholder — Fase 3 agrega tests del orquestador de automatización + Spring AI. */
class SmokeTest {

    @Test
    void sanity() {
        assertThat(1 + 1).isEqualTo(2);
    }
}
