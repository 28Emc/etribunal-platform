server:
  port: 8082
  servlet:
    context-path: /api

spring:
  application:
    name: core-domain-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
